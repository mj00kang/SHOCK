# 샥(SYAK) 서비스 아키텍처 및 상세 기획 명세서 (Service Architecture & Specification)

본 문서는 중고거래에 경매 방식을 결합한 C2C 경매형 중고거래 플랫폼 **“샥(SYAK)”**의 웹 사이트 구현을 위한 서비스 기획 및 프론트엔드/상태 데이터 아키텍처 영속성 설계 문서입니다. 이 문서는 실전 스타트업 MVP 비즈니스의 빠른 검토와 기획 의도를 프론트엔드 컴포넌트 아키텍처로 직결할 수 있도록 설계되었습니다.

---

## 1. 서비스 개요 (Service Overview)

### 1) 서비스 핵심 정의
**샥(SYAK)**은 판매자가 중고상품을 자유롭게 등록하고, 구매자가 입찰(Bid) 또는 즉시구매(Buy It Now)로 거래하는 **경매 중심의 C2C 중고거래 플랫폼**입니다. 

### 2) 비즈니스 해결 과제
*   **가격 흥정 피로 해결**: 기존 중고마켓에서 빈번히 일어나는 비매너 네고(가격 깎기) 요청과 채팅 피로도를 제거합니다. 경매라는 규칙적인 장치를 통해 가격 결정을 시장 원리에 위임합니다.
*   **시세 판단 어려움 극복**: 희소 품목(한정판 스니커즈, 피규어, 빈티지 의류 등)이나 시세 검색이 모호한 상품에 대해 상향식 경매를 활용해 공정하고 투명한 시장 가격(Market Price)을 수렴시킵니다.
*   **거래 속도 지연 해결**: 하향식 경매(Dutch Auction)를 도입하여, 판매자가 설정한 주기마다 자동으로 가격을 떨어뜨림으로써 "오늘 중 무조건 팔아야 하는 물건"의 판매 리드 타임을 극히 최소화합니다.

### 3) 샥(SYAK)의 포지셔닝 (검수 제거 및 C2C 직거래/택배거래 구조화)
*   **탈중앙화/검수 제거**: 샥은 플랫폼이 물리적 물건을 모아 검수하는 무거운 검수 기반 비즈니스(검수 예치금, 보증서, 전문 검수팀 수수료 등)를 완전히 탈피했습니다. 
*   **C2C 에스크로 결제 및 직접 배송**: 구매자는 낙찰 또는 즉시구매 발생 시 안전결제 시스템(가상 에스크로)을 통해 결제하고, 판매자는 등록된 구매자 주소로 직접 배송(택배)하며, 구매자 수령/구매확정 시 판매자에게 가상 정산(LocalStorage 반영)이 완료되는 심플하고 린(Lean)한 구조입니다.

---

## 2. 핵심 사용자 흐름 (Core User Flow)

### 1) 판매자 흐름 (Seller Flow)
1.  **상품 등록**: 상품 정보(제목, 설명, 카테고리, 상태 등) 및 이미지 업로드.
2.  **경매 방식 선택**:
    *   **상향식 경매(Ascending)**: 시작가, 최소 입찰 단위, 즉시구매가(선택), 종료시간 설정.
    *   **하향식 경매(Descending)**: 시작가, 최저가, 차감금액, 차감주기, 종료시간 설정.
3.  **경매 시작**: 시스템에서 해당 상품을 즉시 활성화(live) 상태로 변경하고, 시간에 따른 경매 진행.
4.  **거래 발생**: 
    *   상향식 경매: 종료 시간 도달로 최고가 낙찰자 결정 OR 즉시구매 발생.
    *   하향식 경매: 하락하는 현재 가격에 구매자가 즉시 낙찰(구매) 클릭.
5.  **배송 상태 업데이트**: 판매 완료 알림 확인 후 운송장 번호 입력(`preparing` -> `shipping`).
6.  **정산 완료**: 구매자 수령 및 구매확정 시, 정산 내역에 수수료를 차감한 정산 금액 반영.

### 2) 구매자 흐름 (Buyer Flow)
1.  **상품 탐색/검색**: 정밀한 카테고리, 경매 유형별 필터 및 고성능 검색 기능을 통해 관심 대상 서칭.
2.  **관심상품(찜하기) 등록**: 마음에 드는 상품의 실시간 가격 변동 및 마감 임박 알림 수신 동의.
3.  **경매 참여**:
    *   상향식 경매: `현재가 + 최소 입찰 단위` 이상의 가격을 입력하여 호가 입찰.
    *   하향식 경매: 실시간으로 정해진 주기마다 내려가는 가격을 모니터링하다 원하는 지점에서 "즉시 구매" 클릭.
    *   즉시구매: 상향식 상품에 걸려있는 정가 조건으로 즉각 낙찰.
4.  **낙찰 및 주문 확인**: 낙찰/구매 성공 즉시 알림 수신 및 마이페이지 내 '결제대기' 주문 생성.
5.  **가상 결제 완료**: 마이페이지에서 가상 페이머니 혹은 신용카드 모의 결제를 진행하여 배송 요청 상태(`paid`)로 전환.
6.  **수령 및 확정**: 배송 완료 상태 확인 시 "구매 확정" 버튼을 클릭하여 거래 마무리.

---

## 3. 사이트맵 및 페이지별 기능 상세 (Sitemap & Page Details)

```
[로고 (SYAK)] ———— 3단 완성형 헤더 (검색창 / 전체 카테고리 / 마이페이지, 알림 연결)
   │
   ├─► [홈 (Home)] ────────────────► 비주얼 배너 / 오늘의 경매 / 마감임박 / 가격 내려가는(하향식) / 럭키샥 프리뷰
   │
   ├─► [상품 탐색 (Browse/List)] ────► 검색어 연동 / 카테고리 트리거 / 경매 방식 필터링 / 정렬 (마감 임박순 등)
   │
   ├─► [상품 상세 (Product Detail)] ──► 실시간 타이머 / 경매 유형별 입찰-즉시구매 위젯 / 입찰 히스토리 로그
   │
   ├─► [판매하기 (Sell Product)] ─────► 다이나믹 경매 파라미터 폼 (상향식 / 하향식 조건부 필드 동적 노출)
   │
   ├─► [마이페이지 (Mypage)] ─────────► 입찰 내역 및 낙찰 결제 모듈 / 판매 상품 운송장 제어 / 알림 내역 센터
   │
   ├─► [럭키샥 이벤트 (Lucky Sync)] ──► 100원 래플 응모 / 실시간 응모자 집계 / 당첨자 추첨 및 가상 결제 프로세스
   │
   └─► [관리자 대시보드 (Admin)] ─────► 플랫폼 통계 / 이벤트 신규 등록 및 수동 추첨 / 신고 내역 처리 차단기
```

### 1) 홈 (Home Page)
*   **메인 배너**: 샥 고유의 비주얼 테마(일러스트 또는 프로모션 그래픽) 중심의 슬라이드 배너.
*   **오늘의 경매**: 조회수나 입찰수가 급증하는 대표적인 트렌딩 상품 컬렉션.
*   **마감임박 상품**: 종료 시간까지 남은 시간이 2시간 미만인 상품들을 남은 시간 오름차순으로 배치(실시간 카운트다운 타이머 동작).
*   **가격 내려가는 상품 (하향식 경매)**: 하향식 경매 유형만 분류하여, 현재 진행 상황 및 다음 차감 예정 시간/금액을 실시간으로 갱신하는 큐레이션 영역.
*   **인기 입찰 상품**: 실시간 입찰 횟수(Bid Count)가 가장 높은 상위 아이템 정밀 정렬.
*   **즉시구매 가능 상품**: 즉시구매가(Buy Now Price)가 설정된 상향식 경매 또는 빠른 판매 목적 상품.
*   **럭키샥 이벤트**: 현재 열려있는 100원 래플 이벤트의 남은 시간 및 "응모하기" 퀵 버튼 제공.

### 2) 상품 탐색 (Product List Page)
*   **동적 검색 및 필터**: 상단 헤더 검색어 및 하단 카테고리 드롭다운과 100% 동적 연동.
*   **경매 방식 탭 필터링**: [전체] [추천] [상향식 경매] [하향식 경매] [즉시구매 가능] 필터 제공.
*   **세부 필터 조건**: 상품 상태(S급, A급, B급), 최소/최대 가격 범위 스크롤바 바인딩.
*   **정렬(Sort) 정밀도**: 마감임박순, 입찰수 많은순, 낮은 현재가순, 최신 등록순 정렬 기준 지원.

### 3) 상품 상세 (Product Detail Page)
*   **메인 헤더 상품 정보**: 고품질 업로드 이미지, 상품 상세 스펙, 상태 정보, 판매자 매너 마일리지 표시.
*   **실시간 경매 오퍼레이션 위젯**:
    *   **상향식 위젯**: 현재가, 남은 시간 카운터, 입찰 수, 최소 입찰 단위, 즉시구매가(있을 경우). `입찰하기` 버튼 클릭 시 입찰 금액 입력 하위 서브 팝업/모달 활성화 (현재 최고 입찰자가 본인일 경우 '현재 내가 최고 입찰자입니다' 가이드 제공). `즉시구매` 클릭 시 즉시 낙찰 전이 승인 확인 대기창 호출.
    *   **하향식 위젯**: 현재가(시간에 맞춰 계산된 실시간 하락가), 다음 하락 예정 시간 안내, 최저 한계선(Floor) 표시. `현재가로 즉시 결제/구매` 버튼 1개 집중 노출.
*   **입찰 역사(Bid History)**: 상향식 경매 전용, 어떤 사용자가 몇 시 몇 분에 얼마로 입찰했는지 최신순으로 정렬된 시간순 테이블 리스트 제공 (실시간 생동감 극대화).
*   **관심상품(찜) 토글**: 내 보관함에 즉각 동기화되며, 변동 상황 시 가상 푸시 알림 트리거 연동.

### 4) 판매하기 (Sell Product Page)
*   **기본 인풋**: 제목, 상품 설명, 대표 카테고리 선택, 상품 상태 라디오 그룹(S급 미개봉, A급 사용감 적음, B급 중고감 있음).
*   **경매 유형 라디오 탭**: `상향식 경매` 와 `하향식 경매` 분기 처리.
*   **동적 입력 폼**:
    *   상향식 경매 선택 시: `경매 시작가`, `최소 입찰 단위`, `즉시구매가 설정 유무(체크박스 활성화 시 금액 입력창 오픈)`, `경매 기간(1일, 3일, 5일, 7일)` 필드 렌더링.
    *   하향식 경매 선택 시: `경매 시작가`, `최종 최저 가격(Floor Price)`, `시간당 가격 차감액`, `차감 주기(1분, 5분, 30분, 1시간)`, `경매 전체 종료시각` 필드 렌더링.
*   **등록 유효성 검증**: 비어있거나 불가능한 가격 관계(예: 상향식 시작가가 즉시구매가보다 높음, 하향식 최저가가 시작가보다 높음 등) 입력 시 경고 알럿 또는 유효성 에러 표시 처리 후 `등록 완료` 동작 차단.

### 5) 마이페이지 (My Page)
*   **내 활동 현황 대시보드 요약**: 4개 메트릭 카드로 구성 (입찰 중인 건수 / 낙찰 결제대기 건수 / 판매 중인 경매 / 누적 정산완료 금액).
*   **탭 영역**:
    *   **내 입찰 내역**: 내가 참여한 상향식 경매 리스트, 현재 상태(낙찰 성공-결제대기 / 상위 입찰자 등장(패배) / 경매 진행 중).
    *   **내 구매 내역**: 낙찰 및 즉시구매한 상품의 리스트. '결제대기(unpaid)'일 경우 100원 가상 한도 차감식의 간편 결제 카드 활성화. '배송 준비 중(preparing)' / '배송 중(shipping)'의 실시간 가상 조회 가능 및 '구매 확정' 완료 시 판매자 즉시 정산 연동.
    *   **내 판매 상품**: 내가 등록한 상품들의 실시간 입찰 현황 및 종료 현황. 낙찰된 상품에 대해 운송장 번호 입력 필드 제공 및 배송 시작 버튼 조작 지원.
    *   **관심상품**: 스크랩한 수많은 상품들의 직관적인 바둑판 뷰 및 실시간 남은 시간 타이머 가동.
    *   **알림**: 나에게 발생한 알림 로그 내역 (상위 낙찰 발생, 마감 직전 경보, 이벤트 당첨 통지).
    *   **정산 내역**: 수수료(플랫폼 거래 성사 수수료 3%)를 자동 제한 최종 가상 파이낸스 머니 누적 현황 및 계좌 가상 출금 신청 기능 시뮬레이션.

### 6) 럭키샥 이벤트 (Lucky SYAK Raffle)
*   **콘셉트**: 일반 C2C 경매와 구분되는 어드민 주최 마케팅 이벤트. 트렌디한 한정판 미개봉 상품을 플랫폼이 공급하여 단돈 **100원**에 응모하는 재미 요소.
*   **화면 인터페이스**:
    *   현재 응모 가능한 럭키샥 이벤트 상품들의 남은 시간 정보 및 비주얼 상세.
    *   `응모하기` 버튼: 중복 응모를 차단하기 위해 로그인한 사용자의 고유 UUID를 `participants`배열에 주입, 실시간 동적 참여자 카운트 숫자 상승 처리.
    *   **가상 타임랩스 추첨 시연**: 카운트다운이 종료되거나 관리자가 추첨 버튼을 누르는 순간, 화려한 폭죽 또는 당첨자 추첨 애니메이션을 통해 선정된 당첨 사용자의 닉네임이 메인에 공표됨.
    *   **당첨자 전용 결제 플로우**: 선정된 당첨자가 당첨 알림 확인 후 해당 페이지 방문 시 `100원 결제하러 가기` 버튼이 유일하게 활성화되어 최종 구매 트랙으로 연계.

### 7) 관리자 대시보드 (Admin Dashboard)
*   **종합 대시보드**: 누적 상품 수, 활성 경매 비율, 누적 가상 정산 거래량, 누적 가입자 수, 미처리 신고 수.
*   **회원 관리**: 서비스 블랙컨슈머에 대한 제어(활동 정지/활성화) 및 샥 머니 정산 유효성 점검.
*   **상품 및 경매 제어**: 잘못 등록되거나 불법 물건인 경우 일관적으로 `hidden` 처리하여 탐색에서 격리.
*   **거래 관리**: 누적된 Order 목록을 확인하고, 결제 기한을 임의 연장하거나 강제 취소, 혹은 테스트 배송 수동 완료 지원.
*   **신고 내역 통합 판넬**: 접수된 사기, 장난 입찰, 불법 카테고리 신고서의 일괄 조회 및 즉시 처리.
*   **럭키샥 이벤트 크리에이터**: 럭키샥 한정판 상품명, 응모 시작/종료일, 이미지, 당첨자수 기준을 생성/수정 가능하고, 즉각적인 추첨 테스트 엔진 구동 버튼 탑재.

---

## 4. 주요 기능 모듈 (Software Modules Blueprint)

```
┌────────────────────────────────────────────────────────┐
│                      웹앱 상위 레이어                   │
├────────────────────┬───────────────────┬───────────────┤
│    경매 타이머     │    입찰/결제      │    3단 헤더   │
│   (Timer Module)   │   (Bid/Pay Mod)   │   (Search/Nav)│
└─────────┬──────────┴─────────┬─────────┴───────┬───────┘
          │                    │                 │
┌─────────▼────────────────────▼─────────────────▼───────┐
│              비즈니스 로직 및 트랜잭션 수트             │
├────────────────────────────────────────────────────────┤
│  • auctionUtils / priceUtils 계산 파라미터 엔진        │
│  • useLocalStorage 데이터 무결성 훅                    │
│  • 럭키샥 추첨 난수 분배 시스템                        │
└────────────────────────┬───────────────────────────────┘
                         │
┌────────────────────────▼───────────────────────────────┐
│              LocalStorage 영속성 저장부               │
│     (Users, Products, Auctions, Bids, Orders, etc.)    │
└────────────────────────────────────────────────────────┘
```

### 1) 타이머 정밀 모듈 (`useAuctionTimer`)
*   경매 진행 중 남은 시간($T_{end} - T_{current}$)을 밀리초(ms) 단위로 계산하여, 초 단위의 카운트다운을 화면 프레그먼트에 연속 트리거합니다.
*   종료 시점이 지나가면 데이터 상태(Status)를 지연 없이 `ended` 혹은 최고 낙찰 입찰 존재 여부에 따라 즉시 낙찰 상태(`sold`)로 전환하며, 백그라운드 수동 갱신 없이도 화면이 자동으로 주문 결제 처리로 가동되도록 리렌더링 분기를 제공합니다.

### 2) 입찰 및 머니 트랜잭션 모듈 (`auctionUtils`)
*   새로운 경매 입찰 요청 수신 시, `입찰 가능 여부 제약사항`을 무결하게 확인합니다 (본인 상품 여부, 최고가보다 입찰 단위 이상으로 높은지 여부, 경매 만료 여부 등).
*   안전 결제 시뮬레이션을 구현하여, 가상 머니가 부족하면 결제 대기에서 결제 실패 알럿을 제공하고, 정상 충전/결제 프로세스 수임 시 낙찰 프로세스를 완성합니다.

### 3) 로컬 데이터 동기화 관리기 (`storageUtils`)
*   웹앱 진입 단계에서 defaultMockData를 LocalStorage에 적절히 로드(Hydration)하고, 이후 모든 사용자 등록, 입찰, 알림, 럭키샥 참여 내역을 전역 동기화합니다.
*   멀티플 브라우저 탭 테스트 혹은 단순 새로고침 시에도 중고 판매 상품군과 응모 인원 숫자가 즉각적으로 동결 보존됩니다.

---

## 5. 상향식 경매 (Ascending Auction) 로직 상세

### 1) 상태 전이 시나리오
*   **입고 등록(scheduled/live)**: 등록한 순간 경매가 활성화됩니다.
*   **호가 경쟁**: 구매자들이 지속적으로 이전 입찰가 대비 차등된 높은 가격으로 베팅합니다.
*   **스나이핑(Sniping) 방지 연장 알고리즘**:
    *   경매 마감 직전 1초를 두고 입찰을 가로채는 행위를 방지하기 위해, 만료 시점 전 마지막 **5분** 이내에 새로운 유효 입찰이 접수되면 경매 만료 시점($T_{end}$)이 자동으로 **5분** 전면 연장됩니다.
    *   연장 계산 로직: 
        $$\text{if } (T_{end} - T_{bid} \le 5 \text{ mins}) \implies T_{end} = T_{bid} + 5 \text{ mins}$$
*   **만료 도달 (Timeout)**:
    *   최종 입찰자가 존재하는 경우: 경매 상태는 `sold`, 상품 상태도 `sold`로 자동 변경되며, 최고가 입찰자는 결제 대기 주문(`Order`)을 발주받습니다.
    *   입찰자가 한 명도 없었던 경우: 상태는 `ended`로 격하되며, 미판매 종료 처리됩니다.
*   **즉시구매 발생**: 즉시구매가($P_{buy}$) 조건이 활성화된 상태에서 구매자가 즉시구매 버튼을 타격하면, 남은 경매 시간과 관계없이 즉각적으로 `sold` 전이를 일으키고 낙찰 거래가 잠금(Lock) 처리됩니다.

---

## 6. 하향식 경매 (Descending Auction) 로직 상세

### 1) 시간 차감 공식 및 실시간 연산
하향식 네덜란드식 경매는 백그라운드 크론탭(Cron) 없이 **프론트엔드 실시간 온디맨드(On-demand) 연산**으로 완벽하게 수렴하도록 설계합니다.

*   **현재가 동적 계산 법칙**:
    사용자가 상품 페이지를 열어보거나 목록을 볼 때마다, 경매 시작가($P_{start}$)에서 시작 시간($T_{start}$) 이후 흘러간 시간($T_{current} - T_{start}$)을 차감주기($t_{interval}$)로 나눈 몫에 차감 금액($\Delta P$)을 곱하여 계산합니다.

    $$N_{decrement} = \left\lfloor \frac{T_{current} - T_{start}}{t_{interval}} \right\rfloor$$
    $$P_{calculated} = P_{start} - (N_{decrement} \times \Delta P)$$
    $$P_{current} = \max(P_{floor}, P_{calculated})$$

*   **동작 시나리오 예시**:
    *   시작가: 100,000원 / 최저값(Floor): 40,000원 / 차감금액: 10,000원 / 주기: 10분 일 때,
    *   경매 시작 15분 후: $N = 1$이므로 현재가는 `90,000원`으로 노출됩니다.
    *   경매 시작 65분 후: $N = 6$이므로 계산값은 `40,000원`이 되며 최저값인 40,000원에 도달하여 정지합니다.
*   **종료 처리**:
    *   가장 먼저 "현재가 구매" 버튼을 타격한 사용자 한 명에게 즉시 소유권과 낙찰 권리가 부여되며 경매는 자동 영구 종료(`sold`)됩니다.
    *   만약 최종 경매 예정 종료 시각($T_{end}$)에 이를 때까지 단 한 명도 낙찰받지 않은 채 마감되면, 미판매(`ended`) 처리 상태로 정리됩니다.

---

## 7. 즉시구매 (Buy It Now) 로직 상세

*   **트랜잭션 가로채션 모델**:
    상향식 경매의 호가가 진행되던 한복판에서도, 즉시구매가 존재 상품이라면 구매자가 `즉시구매가 현재 결제`를 선택할 수 있으며, 이 즉각 명령은 다른 경쟁자들의 추가 정규 입찰을 무력화시킵니다.
*   **실행 루틴**:
    1.  사용자가 `즉시구매` 요청 발동.
    2.  로컬 데이터베이스에서 해당 상품의 `status`가 여전히 `live`인지 실시간 체크 (동시 클릭 정밀 제어).
    3.  유효할 경우, 즉시 상품 및 옥션 상태를 `sold`로 스위치하고 바이어를 등록.
    4.  이전 최고 입찰자에게는 '타인의 즉시구매로 인해 아쉽게 유찰되었습니다'라는 가상 시스템 알림 송출.
    5.  즉시구매 신청자 전용 주문(`Order`)을 최종 확정 갱신.

---

## 8. 럭키샥 이벤트 로직 상세

기존 불확실한 도박형 경매 참여와 달리, 플랫폼 고유 복권형 래플 형태인 마케팅 도구로서 럭키샥은 다음과 같이 엄격하게 전개됩니다.

```
[럭키샥 개시] (어드민 / 100원 가격 지정)
    │
    ├─► [유저 응모] (중복 차단 및 participants 배열에 유저ID push)
    │
    ├─► [시간 종료 / 어드민 수동 추첨 타격]
    │
    ├─► [랜덤 피커(Random Picker) 엔진 가동]
    │      Array 내 인덱스를 난수(Math.random)로 선정하여 Winners 리스트 산출
    │
    ├─► [상태 전환] LuckyEvent status -> 'drawn'
    │
    └─► [당첨자 가상 결제] 당첨 유저 마이페이지 결제 위젯 오픈 -> 100원 결제 완료 -> 배송 처리
```

*   **당첨자 필터링**: 당첨되지 않은 사용자의 마이페이지에는 응모 종료 및 탈락 피드백을 전달하여 불필요한 결제를 사전 차단합니다.
*   **추첨 무결성**: 로컬스토리지 기반 난수 추출 시스템이 작동하므로 새로고침 및 데이터 조작으로부터 불변이 유지되도록 추첨 결과를 로컬 데이터 모델 `LuckyEvent` 필드의 `winners`에 직접 명문화하여 저장합니다.

---

## 9. 데이터 모델 (Analytical Data Model)

웹앱 데이터 무결성 유지를 위해 설계된 TypeScript 중심 필드 모델 설계입니다.

### 1) User
```typescript
interface User {
  id: string;          // 고유 식별자 (UUID)
  nickname: string;    // 사용자 닉네임
  email: string;       // 사용자 이메일
  role: 'user' | 'admin'; // 일반 유저 / 플랫폼 최고 관리자 구분자
  profileImage: string;// 프로필 이미지 URL 혹은 아바타 코드
  createdAt: string;   // 가입 날짜 ISO
}
```

### 2) Product
```typescript
interface Product {
  id: string;          // 상품 ID
  sellerId: string;    // 판매자 User ID
  title: string;       // 상품 제목
  category: string;    // 카테고리 (전자기기, 패션/잡화, 명품, 생활가전 등)
  description: string; // 상품 본문 상세 설명
  images: string[];    // 업로드된 이미지 URL 목록 (또는 가상 프리셋 키)
  condition: 'S' | 'A' | 'B'; // S급(미개봉), A급(깨끗함), B급(사용감)
  auctionType: 'ascending' | 'descending'; // 두 가지 결합 경매 유형
  status: 'draft' | 'live' | 'sold' | 'ended' | 'cancelled' | 'hidden';
  viewCount: number;   // 실시간 카운트용 가상 조회수
  likeCount: number;   // 찜 횟수
  createdAt: string;   // 등록일
}
```

### 3) Auction
```typescript
interface Auction {
  id: string;          // 경매 고유 매핑 ID
  productId: string;   // 연계 상품 외래키 ID
  auctionType: 'ascending' | 'descending';
  startPrice: number;  // 시작 기준 가격
  currentPrice: number;// 실시간 동적 연산 혹은 호가 반영가
  buyNowPrice?: number;// 상향식에서 즉시구매 가능 가액 (Optional)
  minBidIncrement?: number; // 상향식의 최소 입찰 단위 단계 (Optional)
  floorPrice?: number; // 하향식의 최저 한계 가격 (Optional)
  decrementAmount?: number; // 하향식의 단계별 차감 금액 (Optional)
  decrementInterval?: number; // 하향식의 가격 차감 단위 분/초(분 단위 변환 권장) (Optional)
  startAt: string;     // 경매 개시 시각
  endAt: string;       // 경매 만료 시각
  status: 'scheduled' | 'live' | 'ended' | 'sold' | 'cancelled';
}
```

### 4) Bid
```typescript
interface Bid {
  id: string;          // 개별 호가 입찰 ID
  auctionId: string;   // 대상 경매 ID
  productId: string;   // 연계 상품 ID
  bidderId: string;    // 입찰을 추진한 User ID
  bidPrice: number;    // 입찰 제시 금액
  createdAt: string;   // 입찰 시각 ISO
}
```

### 5) Order
```typescript
interface Order {
  id: string;          // 거래 주문서 고유코드
  productId: string;   // 타겟 상품 ID
  auctionId: string;   // 연동 경매 ID
  buyerId: string;     // 최종 낙찰 구매자 ID
  sellerId: string;    // 등록 판매자 ID
  finalPrice: number;  // 거래 타결된 최종 체결 가격
  orderType: 'bid_win' | 'buy_now' | 'lucky_win'; // 일반낙찰 / 즉시구매 / 럭키샥 구분
  paymentStatus: 'unpaid' | 'paid' | 'refunded' | 'failed';
  deliveryStatus: 'preparing' | 'shipping' | 'delivered';
  orderStatus: 'pending_payment' | 'paid' | 'shipping' | 'delivered' | 'completed' | 'cancelled';
  createdAt: string;   // 생성시각
}
```

### 6) Notification (알림 객체)
```typescript
interface Notification {
  id: string;
  userId: string;      // 해당 알림 수신 대상 User ID
  type: 'bid_success' | 'outbid' | 'auction_won' | 'product_sold' | 'ending_soon' | 'buy_now_success' | 'lucky_event_joined' | 'lucky_event_won';
  message: string;     // 화면 출력용 한글 알림 메시지 문자열
  isRead: boolean;     // 읽음 플래그 처리
  createdAt: string;
}
```

### 7) LuckyEvent
```typescript
interface LuckyEvent {
  id: string;
  title: string;       // 래플 타이틀 (e.g., "맥북 프로 M4 100원 래플!")
  productName: string; // 상품 정형 이름
  image: string;       // 럭키샥 이미지
  eventPrice: number;  // 항상 100원 고정 설정
  startAt: string;
  endAt: string;
  winnerCount: number; // 당첨 선발 인원
  participants: string[]; // 참여 응모한 유저 ID 배열
  winners: string[];   // 선발 추첨이 종료되어 확정된 당첨 유저 ID 배열
  status: 'upcoming' | 'live' | 'ended' | 'drawn' | 'cancelled';
}
```

### 8) Report (사기 및 불통 회원 정밀 신고)
```typescript
interface Report {
  id: string;
  productId: string;   // 대상 상품
  reporterId: string;  // 신고자
  reason: string;      // 사유 문자열
  status: 'pending' | 'resolved' | 'dismissed';
  createdAt: string;
}
```

---

## 10. 상태값 설계 정의 (State Machine Blueprint)

### 1) Product & Auction 동기화 라이프사이클
```
[판매글 드래프트(draft)] ──(공개등록)──► [게시 및 진행(live)]
                                              │
                    ┌─────────────────────────┴────────────────────────┐
             (종료 직즉시구매 / 낙찰체결)                                (입찰 제로 마감)
                    ▼                                                  ▼
          [판매완료(sold)]                                         [미판매종료(ended)]
```

### 2) Order & Delivery 상세 트랜지션
```
[결제대기(pending_payment)] ──(결제액 지출)──► [결제완료(paid)] ──(운송장 입력)──► [배송중(shipping)] ──(수령확인)──► [배송완료(delivered)] ──(확정성공)──► [거래종료(completed)]
```
*   **패널티 자동 감지**: 만약 `pending_payment` 시각에서 만료 기한(가상 3일 혹은 어드민 강제 종료 시) 내 결제가 부재할 때 상태가 `cancelled`로 자동 회향하며 낙찰자 신용점수에 차감을 기록합니다.

---

## 11. 관리자 기능 설계 명세 (Admin Operations Guide)

어드민 화면은 허울뿐인 페이지가 아닌 다음과 같은 완제품 수준의 로컬 가상 백오피스 액트 인터페이스를 구가합니다.

*   **실시간 어드민 제어 센터**:
    *   **신고 패널 및 차단 연동**: 유저 대화에서 이상 징후가 신고되거나 장난 입찰 상품이 신고될 시, 버튼을 클릭하여 즉각 해당 세일 상태를 `hidden` 혹은 `cancelled` 처리합니다.
    *   **럭키샥 래플 라이프사이클 제어**: 
        1. 어드민 폼을 통해 임의의 최신 한정판 경품 100원 래플을 목록에 바인딩합니다.
        2. "수동 추첨 즉시 진행" 버튼을 클릭하면, 현재까지 응모된 참가자 목록 `participants` 배열 전체 중 특정 요소를 랜덤 샘플링하여 즉각 `winners` 리스트를 확정 갱신합니다.
    *   **강제 유찰 및 거래 재가동**: 낙찰자가 패널티 상태인 경우 단 한 번의 버튼 클릭으로 해당 경매를 유찰 및 취소 처리하여 판매자에게 소유권을 즉시 반환 지원합니다.

---

## 12. MVP 우선순위 수립 (MoSCoW Matrix)

*   **Must Have (필수 고품질 가이딩)**:
    *   상향식 경매 정밀 입찰 및 실시간 연장 수치 계산
    *   하향식 경매의 실시간 계산식 가격 표출 및 결제 수임
    *   로컬스토리지를 활용한 상태 보존 및 상품 추가
    *   마켓컬리를 변용한 완성도가 높은 3단 고성능 헤더
    *   럭키샥 이벤트 응모 및 관리자 가상 추첨 시나리오
*   **Should Have (높은 권장 사항)**:
    *   실시간 반응형 애니메이션 트랜지션(framer-motion 활용)
    *   마이페이지 실거래 탭 가상 통계 구현
    *   상품 상세 페이지에 실시간 입찰 시간 순 히스토리 테이블 정렬
*   **Could Have (후순위 구현 가능 옵션)**:
    *   인공지능 가상 감정사 챗봇(Gemini API API를 우회 설계해 입찰가 어드바이스 제공)
*   **Won't Have (이번 플랫폼 범위에서 절대 제외)**:
    *   **실제 검수 업체 중개 및 검수 감정서 발행 흐름**
    *   **검수 수수료 정산 및 예치금 실서버 연동 결제**

---

## 13. 향후 React/Vite 구현 시 파일 구조

추후 개발될 완벽한 샥(SYAK) 프로젝트 구성 기본 뼈대입니다.

```
/src
  ├── main.tsx                # React 앱 시작 진입점
  ├── App.tsx                 # 핵심 리액션 허브 및 뷰 분기 라우터
  ├── index.css               # 테일윈드 4 스타일 및 보라색/블랙 그라데이션 커스텀 지정
  │
  ├── data/
  │     └── mockData.ts       # 초기 유저, 상품군, 하향식 경매, 럭키샥 이벤트 풍부한 기본 컬렉션
  │
  ├── utils/
  │     ├── auctionUtils.ts   # 상향식/하향식 현재가 및 호가 검증 계산 로직
  │     ├── storageUtils.ts   # LocalStorage 무결성 안전 영속 입출력
  │     └── priceUtils.ts     # 원화(KRW) 포맷 화폐 변환 및 퍼센테이지 차감 뷰어
  │
  ├── hooks/
  │     ├── useLocalStorage.ts # 로컬스토리지 전역 동기화용 커스텀 심플 훅
  │     ├── useAuctionTimer.ts # 고해상도 경매 마감 타이머 처리기
  │     └── useNotifications.ts # 실시간 가상 푸시 알림 디스패치 제어
  │
  ├── components/
  │     ├── Header.tsx        # ★ 마켓컬리 양식을 현대적 보라빛으로 전면 재해석한 3단 완제품 헤더
  │     ├── Footer.tsx        # 플랫폼 하단 신뢰도 정제 풋바
  │     ├── ProductCard.tsx   # 경매 진행 바가 실시간 출렁이는 그리드 전용 카드
  │     ├── AuctionTimer.tsx  # 깜빡이며 위기감을 주는 카운트다운 컴포넌트
  │     ├── BidPanel.tsx      # 상향식 입찰 다이얼로그 및 오류 체크 패널
  │     ├── LuckyEventCard.tsx # 100원 래플 상황 시그널 및 응모 프로그레스
  │     └── StatusBadge.tsx   # 경매 상태별 미니멀 칩 뱃지
  │
  └── pages/
        ├── Home.tsx          # 샥 메인 홈 광장
        ├── ProductList.tsx   # 전력 통합 정렬 필터 기능 상품 목록 관문
        ├── ProductDetail.tsx # 상하향식 액션 바인딩 상품 정밀 디테일 뷰
        ├── SellProduct.tsx   # 스위치형 경매 셋업 상품 출품 양식장
        ├── MyPage.tsx        # 가상 머니, 낙찰 결제, 내 판매 및 알림 통제 마이룸
        ├── LuckyEvent.tsx    # 100원 럭키샥 래플 응모 광장
        └── Admin.tsx         # 통계 시각화 및 신고, 상품 통제, 럭키샥 즉시 추첨 백오피스
```

---

## 14. 검수 제거 후 최종 서비스 정의

본 플랫폼은 플랫폼이 직접 상품을 검수하는 비효율적이고 무거운 구식 위탁 모델(예: 검수비, 예치 보증, 물리적 입출고 기록)을 완벽히 소거했습니다. 대신 다음과 같은 장치들을 통해 플랫폼 신뢰도와 투명성을 실현합니다.

1.  **실시간 상시 신고 시스템 (Report Interlock)**: 과장된 상품 상태, 유령 입찰, 연락 두절 등 부정한 판매 행위를 구매자가 단 한 번의 클릭으로 신고할 수 있으며, 관리자 백오피스에서 클릭 한 번으로 상품과 경매를 동결시킬 수 있습니다.
2.  **안전 결제(에스크로) 가상 모델**: 구매자가 결제한 대금은 직접 거래 확정 전까지 플랫폼 에스크로 영역에 안전하게 보관되며, 배송 수령 및 구매 확정이 승인되어야만 판매자의 가상 정산 지표에 한도액이 정산됩니다.
3.  **판매자 평판 및 매너 스코어 시스템**: 사후적 인스펙션 대신, 이전 거래의 구매 확정률, 배송 속도에 기반한 매너 지수를 노출시켜 안전거래 환경을 자연스럽게 조성합니다.

---

## 15. 헤더 및 상단 네비게이션 디자인 기획 상세

마켓컬리의 정돈되고 우수한 UI 컴포넌트 구성 요소의 계층을 물려받아, 샥(SYAK) 고유의 트렌디하고 역동적인 테크 테마로 구현합니다.

*   **컬러 프로필**: 텍스트 및 그리드는 딥 블랙(Charcoal `#111115`)과 화이트(`#FFFFFF`) 바탕에 포인트 컬러인 **일렉트릭 바이올렛 (Electric Violet `#7C3AED`)**과 **옥션 네온 블루 (`#06B6D4`)**를 매칭하여, 감도 높은 핀테크/리셀 플랫폼 전경을 설계합니다.
*   **3단 완전 레이아웃 상세**:
    1.  **1단: 최상단 미니 유틸리티 (Height: 32px, Bg: Soft Neutral Grey)**
        *   비로그인 시: `회원가입` | `로그인` | `고객센터` 가독성 뛰어난 서체 배치.
        *   로그인 시: `마이페이지` | `알림 (안읽은 메시지 수 지표 메탈 배지)` | `로그아웃` | `고객센터`.
    2.  **2단: 메인 아이덴티티 허브 (Height: 88px, Bg: White, Border bottom: Soft Grey)**
        *   **로고**: 샥 로고(SYAK 기하학 폰트, `#7C3AED` 테마 색 적용)를 좌측에 배치하고 메인 홈 이동 바인드.
        *   **중앙 와이드 검색창**: 가로 영역 480px 이상의 둥근 모서리 검색 바. Placeholder: *"상품명, 브랜드, 카테고리를 입력하고 바로 입찰하세요!"* 우측에 우아한 검색 아이콘 추가 및 동적 타겟 키워드 필터링 즉각 개시.
        *   **우측 액트 전초 기지 아이콘**: 한눈에 들어오는 가독성 높은 아이콘 세트 (벨 모양 `알림센터`, 하트 모양 `관심 목록`, 사람 모양 `마이페이지`). 추가로 주황색 라이브 그라데이션 포인트가 가미된 **'판매하기'** 출품 퀵 버튼을 배치하여 높은 출품 유도.
    3.  **3단: 스펙트럼 네비게이션 라인 (Height: 52px, Bg: White, Sticky 고정)**
        *   **전체 카테고리 햄버거 메뉴**: 호버 혹은 클릭 시 전자기기, 패션 등 모든 도메인 드롭바가 오버레이 형태로 전개되는 액팅 구현.
        *   **수평 링크 바**: `전체상품` | `상향식 경매 🔥` | `하향식 경매 📉` | `마감임박 ⏰` | `즉시구매 ⚡` | `럭키샥 100원 🎁` | `판매하기` 배치. 클릭 시 페이지 전환 없이 홈 화면의 탭 필터를 동적으로 즉각 스위칭하며 부드럽게 스크롤 혹은 데이터 필터 상태를 렌더링.
        *   **끈끈이 헤더(Sticky Header)**: 스크롤이 하단으로 내려갈 시, 3단 네비게이션 라인(또는 미니멀화된 2단 유틸)이 브라우저 최상단에 고정 밀착되어 사용자 친화적 검색 및 탭 서핑 권리를 보전하게 설계합니다.

---

본 상세 서비스 사양 설계 문서를 기반으로, 지연 없이 고품격 경매형 중고거래 플랫폼 샥(SYAK)의 완전 작동하는 웹사이트 구현 단계로 진입할 준비가 되었습니다.
